// Extend timeout for this route — needed for AI image processing
export const maxDuration = 300;

export default function LesetreningLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
